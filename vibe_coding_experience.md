# 重塑创造力：当产品经理转向 Vibe Coding

作为一名兼具商业视角与独立开发经验的产品顾问，我的技术栈曾穿梭于 Python 的数据处理、C# 的强类型业务逻辑或是 Unity 的复杂物理系统中。在过往的独立产品构建序列里，极大的精力往往被环境依赖、API 联调与晦涩的框架配置无尽地吞噬。创造的快乐，常在漫长的前置“搬砖”中被消磨。

直到近期，通过一种被称为 Vibe Coding（基于高维自然语言交互与 AI 辅佐代码生成）的新建构范式，我完成了一款名为「Nest」的个人系统。这是一款离线优先（Local-First）的“家庭数字孪生”（Home Digital Twin）应用。

回顾这段重塑产品构建流的经历，它带来的绝不仅仅是技术栈壁垒的消解，更在于它要求我们对“创造”本身进行重新定义。在这个范式下，我们正从系统代码的“执行流编写者”，逐步转变为宏观规则的“架构指挥家”。

---

## 需求发现：「笨蛋家居」的局限性

市面上的智能家居平台在设备互联和自动化场景上已经极其成熟，但在日常生活中，依然存在大量“无法联网”的监控死角：

- 饮用水的存量与自然去化天数。
- 储物柜内猫砂、纸巾等消耗品的去化速率与补货警戒限。
- 宠物重点活动区域与特定空间清洁度的衰减曲线。

我将这类极易被遗忘、重度依赖直接物理介入的场景称为「笨蛋家居」。针对这些场景，我放弃了增加重量级传感器或冗杂云端调用的路线。相反，我期望的解法更偏向于严密的软件工程：摒弃中心化硬件网络，通过极简的锚点动作（例如“换水”、“开封”），由本地精确的数学模型来独立推演家庭资源的真实状态。

在过去，单单是水务管理的时间衰减算法、基于 60 天滑动窗口的库存预测，乃至引入环境参数（如家庭人数、作息乃至宠物负载系数）的顶层架构设计，就需要耗费数周的业余时间进行前后端联调。而借助 Vibe Coding，实现路径发生了本质变化。

---

## 范式转移：工程控制力与系统演化

在 Nest 的完整推进周期中，我的核心职能依旧是产品经理，不再被特定的技术壁垒所约束。我的主要工作彻底归位于：**产品边界界定、模块拆分、业务设计与产品验收**。

传统开发中，这些职能往往以文档的形式流转给研发团队；而在 Vibe Coding 模式下，面对具备“发散”且存在“黑盒”属性的 AI，我需要建立一套极其严密的工程控制流。这趟独立开发之旅，大体包含四个递进的阶段：

### 1. 逻辑推演与架构设计

开发的第一步，并非让 AI 生成任何代码，而是进行业务的底层逻辑推演。

以饮水消耗为例，我需要推理物理常识如何被数字化：
1. 冷启动饮水量按照居民建议日饮水量计算。
2. 日消耗量随常驻人口基数浮动，且在夜间睡眠时需终止消耗。
3. 根据用户的换水时间，与饮水习惯进行对比，同步修正消耗量。
4. 规避异常消耗情况对饮水习惯的影响，如短时间大量饮水、长时间未换水。

推断出这些业务逻辑后，我反向拆解了平台所需的底层架构支持（数据看板、业务模块、参数配置、家庭管理等）。随后，所有逻辑与结构被落成详尽的 PRD（产品需求文档），同时自动生成相关的测试用例。这一步最大的工程价值在于**剥离 AI 的黑盒属性**——用严格的文档作为约束，为系统确立了唯一性。

### 2. 高温探索：发散创造力与视觉定调

当整体蓝图确立，最棘手的工程问题在于长文本带来的 Token 过载，这极易导致 AI 生成中途崩乱。因此，我制定了“分治化”的开发策略。

第一步是在 AI Studio 中开启一个独立探索分支，并刻意**调高了模型的 Temperature**，设定角色为前端开发以及视觉设计师。
我将初步的产品框架输入，释放其创造力，进行 UI 风格的随机“抽卡”。此阶段我不追求代码的绝对稳定，而是用极低的成本去试探并敲定最佳的设计语言（如拟物玻璃化、特定的交互反馈）。该分支的唯一目的是完成视觉与基础交互的定调。确定完成后，将既有设计语言用代码固化保存，为下一个迭代做准备。

### 3. 低温基建：收束变量与搭建底层骨架

在确认了设计语言后，我随之切入第二个核心分支，**将 Temperature 限制在较小的范围**，要求 AI 回归系统架构师角色。

在此分支下，我要求它严格依据 PRD 搭建最稳定的平台底层框架。重点在于预留代码结构——为后续的分业务逻辑（水务、宠物、库存）提前预留项目框架，底层代码等。这一阶段打下的“地基”，实质上是对后续所有新模块的代码进行了强制规范。

### 4. 迭代开发：搭建业务与测试

在地基稳固后，开发进入了模块填充期。这同样是一个极易失控的阶段。为了对抗 Token 消耗与 AI 记忆衰退，我的策略是：每次均开启新的迭代分支，将开发粒度拆解到最小。

每一次新模块的开发，我都会执行一套严密的标准动作：
- 更新 System Instruction（系统提示词），将本次迭代开发内容框架进行概述，同时上传 PRD 文档，用来限制后端运算逻辑。
- 加入强制隔离指令：“**严禁修改或重构已有的旧代码结构，如有必须修改的内容请与我讨论**”。
- 当 AI 完成单模块的编写后，我通过上传用例文件，由 AI 自主测试，并设计前端后门进行手工走查验收。

确认无误后，再提交合并新分支。通过这种严密的“system instruction 更新 -> 更新指令 -> 快速迭代 -> 测试 -> 创建新分支”循环，我最终在 AI Studio 上收敛出了一个相对稳固的 Web 端系统。

### 5. 跨端迁移：向本地 IDE 的迁移与 iOS 原生集成

在完成 Web 端的开发后，我依然没有完成最初的目的，在手机上应用。在线 AI 平台虽然在前期具备极高的原型产出效率，但受限于固定的技术栈，无法直接支撑 iOS 平台的原生开发与技术选型调用。

这是在前期工具选型中未能充分预判到的：在线平台即取即用非常方便，甚至多次迭代我是在手机上完成的，但技术上限也极为明显。若在初期能更完善的对开发方向进行合理规划，全部工程本可直接在本地 IDE 内进行开发。

面对跨端代码重构需求，工程不可避免地进入了最为反复的阶段。项目代码完整拉取至本地环境后，即便引入 iOS 开发相关的 Skill（技能树），AI 依然多次出现了底层架构误判，二次修改代码进行创作等问题。最终，通过上传 PRD 、反复强调 0 修改现有后端逻辑，分模块独立设计并建立完成 task 后再开发，才算基本将本地环境稳定下来，实现了 iOS 转译。

过程虽然折磨，但当一个想法，真正落地成为手机里应用时，一切都是值得的。

---

## 问题与思考

这种高度抽象的协作模式绝非完美无缺。在享受高杠杆产出的同时，开发者同样面临环境和工具本身的局限性。脱离了营销文章的过度包装，以下几点是实际落地中无法回避的摩擦区：

1. **Token 消耗带来的局部“降智”**：与日常闲聊的 Agent 不同，系统级的代码开发对文本上下文窗口（Token）的消耗是巨大的。长程对话极易导致信息遗忘或逻辑脱节，继而导致 AI 发生“降智”或产生幻觉。解决思路必须是：尽可能地进行任务拆分，少量多次的提问来维护代码质量。
2. **网络环境的稳定性依赖**：Vibe Coding 高度绑定在线服务的响应速度。频繁的网络掉线会导致中断和代码拼接错误。在本地开发中，需要提前处理好网络代理配置。
3. **不可忽视的技术选型前瞻性**：工具极大降低了代码编写门槛，却抬高了架构的门槛。即使你不懂底层代码实现，也必须在启动初期与 AI 深入探讨并敲定“技术选型”（如是否需要跨端、数据库选型）。一旦选错工具链路线，后期的迁移成本将呈指数级放大。
4. **及时接管的沉没成本阻断**：AI 并不是全能的，无论需求大小，遇到模型兜圈子是很常见的，切忌陷入无意义的拉扯。最高效的方法是直接亲自接管代码进行修改，或者换其他大模型。
5. **文档锚点的绝对控制要求**：切勿依赖大模型脆弱的上下文或跨对话记忆去维系整体系统。在每一次进行新分支开发时，必须准备结构明确的 PRD、架构文档或系统变量说明，并作为强制的 System Instruction 喂给系统。它是对齐系统意图和防止功能发散的基础锚点。
6. **警惕嵌套调用的逻辑逃避**：在处理核心计算或业务推演时，需要审查代码，避免 AI 因为难以实现复杂算法而“偷懒”去调用其他云端大模型 API。我们要的是明确的本地化代码流与数学模型，任何脱离项目（尤其如离线优先架构）将计算卸载到第三方黑盒服务的企图，都需要被扼杀在萌芽中。

---

## 结语：能力内核的深层置换

通过 Nest 这一场从概念验证到工具上线的项目经历，我对 AI 协作的认知趋于有了更完善的认知，甚至反向的规范了作为产品产出的工作路径。

在 AI 辅助开发的重塑下，工具属性被无限平权，开发产品的核心护城河已经发生置换。它不再单纯是“手写代码的流利度”或“API 库的记忆储备”，而是转向**“对所求解问题的认知清晰度”**与**“顶层系统的架构把控水平”**。在这个 Workflow 中，AI 是一个拥有无限续航力的高级执行层，而决策的神经中枢与创造力底盘，只能是你本人。

如果您同样是一位非开发出身的产品顾问、PM，或是脑中满载灵感却受困于技术实现边界的创作者，极力建议尝试 Vibe Coding（但不要轻信营销文章，实际开发时间不会短到离谱）。摆脱繁文缛节，卸下“文档撰写者”的被动标签，去利用更底层的思考和精确的表达，成为真正的产品设计师。